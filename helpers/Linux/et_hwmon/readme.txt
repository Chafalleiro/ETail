🎯 Usage Instructions
Make scripts executable:
chmod +x install_hardware_monitor.sh create_systemd_service.sh check_monitor.sh


Run the complete installer:

sudo ./install_hardware_monitor.sh

Or just create the service (if executable already installed):

sudo ./create_systemd_service.sh


Check status anytime:

./check_monitor.sh
